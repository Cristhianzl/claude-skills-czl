# OS cruft
.DS_Store
Thumbs.db

# Local dev convenience (this repo dogfoods its own config via a symlink)
.claude

# Private / internal — never publish
CZL/
*-private/
*.private.md

# Python caches (from the hooks)
__pycache__/
*.py[cod]

# Archives / backups
*.zip
*.bak
